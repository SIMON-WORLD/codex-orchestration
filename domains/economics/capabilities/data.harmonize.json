{
  "id": "economics.data.harmonize",
  "domain": "economics",
  "description": "Explicit, specification-driven structural data harmonization. V1 supports ONLY declared operations on a frozen-source workflow: exact column rename; declared type coercion (numeric/integer/string/boolean/category); deterministic key normalization (trim/case/zero-pad); exact code/category mapping (no fuzzy); explicit date/year normalization; explicit unit conversion (source/target unit + factor); deterministic declared merge (1:1 / 1:m / m:1; m:m fails closed). It NEVER infers cleaning rules, NEVER imputes/winsorizes/deletes outliers, NEVER decides sample exclusion, NEVER defines treatment, NEVER constructs regression variables, and NEVER applies an eligibility flag to a final sample. Scientific decisions (sample exclusion / treatment definition) remain with the Director/study design. General formula-derived variables belong to economics.data.construct (M2).",
  "risk_level": "medium",
  "methodology": {
    "references": [
      { "name": "DIME Data Handbook (World Bank) - data management / documentation / reproducibility", "url": "https://worldbank.github.io/dime-data-handbook/" },
      { "name": "DIME Wiki - Data Management (deduplication, documentation, data cleaning discipline)", "url": "https://dimewiki.worldbank.org/Data_Management" }
    ],
    "note": "Structural, declared, auditable transformation. Consumes a frozen source + an explicit Domain harmonize plan; fails closed on ambiguity / scientifically material sample-affecting operations. No automatic cleaning/reparation; no invented rules."
  },
  "implementations": [
    {
      "id": "data.harmonize.python.pandas",
      "kind": "tool",
      "name": "pandas",
      "runtime": "python",
      "verification_status": "experimental",
      "environment_requirements": { "runtime": "python", "packages": { "pandas": ">=2.0" } },
      "verification": {
        "evidence": "M1 implementation: Python/pandas read-only harmonization of a frozen synthetic source against an explicit Domain harmonize plan; verifies declared input SHA256 (source mutation fails closed), applies only declared operations in deterministic order, writes a separate deterministic harmonized output, and emits a machine-readable execution log (per-op status, row/col counts, coercion/code failures, merge matched/unmatched, warnings). Frozen evidence: deterministic synthetic harmonize benchmark (rename/type/key/code/date/unit/merge 1:1) PASS; adversarial + provenance/tamper suite PASS. Does NOT impute/winsorize/delete-outliers/decide-sample-exclusion/define-treatment. NOT tested; NOT verified; experimental.",
        "benchmark_ref": "domains/economics/benchmarks/data_harmonize/"
      }
    }
  ],
  "scientific_preconditions": [],
  "decision_requirements": ["variable_definition"],
  "fallback_policy": "needs_decision"
}